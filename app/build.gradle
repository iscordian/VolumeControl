android {
    compileSdkVersion 30
    namespace "com.koushik.volumepanel"

    defaultConfig {
        applicationId "com.koushik.volumepanel"
        minSdkVersion 21
        targetSdkVersion 30
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
